# ACCOUNT_REDESIGN_DESIGN.md — редизайн «Мого акаунта» (/profile)

> Статус: **затверджено CEO 05.08.2026 («роби»). Фази 1–4 ЗАВЕРШЕНО, ЗАКОМІЧЕНО локально
> (20a5efe, 74af3bf, 8d8ceea, b3e2e49 + доки), НЕ запушено.** Лишились «Виплати» (зняті CEO) і heatmap.
> Вкладку «Виплати» CEO зняв зі скоупу фази 2 («виплати поки не роби») — див. §5.1.
> Джерело правди по UI: артефакт з мокапами — https://claude.ai/code/artifact/53438662-0817-4bd4-96ff-edfdb4b4a945
> (секція «Рекомендація в зборі»: екран «Огляд» + екран «Налаштування» + план фаз).

## 1. Мета

Акаунт перестає бути анкетою і стає «моїм зрізом» CRM (employee self-service, патерн BambooHR/Personio):
хто я → що в мене відбувається → мої відсутності/виплати → як система зі мною говорить.

## 2. Цільова структура

Вкладки в тулбарі (SEGMENTED_GROUP, як у `TeamPage`): **Огляд · Відсутності · Налаштування**.
Вкладка «Виплати» спроєктована, але не реалізована — зачекає на рішення §5.1.

- **Огляд:** айдентика (аватар, чипи ролі, контакти) + таймлайн співпраці (старт → випробувальний → сьогодні),
  блок «Сьогодні» (мої задачі/нагадування), баланс відсутностей, мої заявки, виплати (гейтовано), heatmap активності (пізніше).
- **Відсутності:** мої заявки + баланс + `AbsenceDialog` (все вже існує в Команді — тут лише self-зріз).
- **Виплати:** мій зріз «Виплат команді» з Фінансів. **Гейт:** картка видима лише за module_access `finance`
  (реєстр `src/lib/moduleAccess.ts`) — доки CEO не вирішить питання видимості зарплат (див. §5).
- **Налаштування:** картки з власними футерами-«Зберегти» (Vercel-патерн): Особисті дані, Безпека (пароль, сесії),
  Telegram, Сповіщення.

## 3. Фази

### Фаза 1 — квік-віни (DONE, 20a5efe)

- `updateProfile` без `window.location.reload()` — шапка/меню оновлюються подіями `profile:name-updated` / `profile:avatar-updated`.
- Глобальна кнопка «Зберегти зміни» → футер картки «Особисті дані» (зберігається лише те, що редагується).
- Зміна пароля — інлайн-діалог (`supabase.auth.updateUser({ password })`), `/reset-password` лишається для forgot-flow.
- З героя видалено хардкод «Kyiv, UA» і плитки Старт/Стаж/Статус (дубль блоку «Робота в компанії»).

### Фаза 2 — каркас і Огляд (DONE, 74af3bf)

- Вкладки через `usePageHeaderActions` + `UnifiedPageToolbar`; стан у `?tab=` (`absences` / `settings`),
  лічильник заявок на погодженні у вкладці «Відсутності».
- Айдентика + таймлайн співпраці (старт → випробувальний → сьогодні) замінили блок «Робота в компанії».
- «Сьогодні»: нагадування `customers`/`leads` за `manager_user_id` у вікні доби, активні прорахунки
  (`listQuotes` з `managerUserId`), активні дизайн-задачі (`activity_log`, `metadata->>assignee_user_id`,
  `ACTIVE_DESIGN_STATUSES`). Запити гейтяться `tab === "overview"`.
- Відсутності: `listTeamAbsencesForUserYear` + `loadAbsenceBalances` + `AbsenceBalanceMeters` +
  `AbsenceDialog` у режимі `request`; скасування через `cancelOwnAbsenceRequest` + `notifyAbsenceRequestCancelled`.
- **ГОЧА, яку легко зламати:** `teamId` (з `useAuth`) — для data-таблиць (customers/leads/quotes/activity_log),
  `resolveWorkspaceId` — для відсутностей/квот/директорії. Плутанина цих двох дає порожні списки без помилки.
- Дедлайн дизайн-задачі сортується на клієнті: він у JSON (`metadata.design_deadline` з фолбеком на `deadline`),
  серверний `.order()` по ньому неможливий.

### Фаза 3 — решта «Налаштувань» (DONE, 8d8ceea)

- Особисті дані з футером-збереженням, Безпека (інлайн-пароль), Telegram, лінк-картка «Центр сповіщень».
- Сесії: показуємо рівно одну — поточного браузера (UA + `session.user.last_sign_in_at`).
  **`signOut({ scope: "others" })`, а не `global`** — кнопку тиснуть саме в тій сесії, яку хочуть лишити;
  `global` викинув би людину звідси разом з рештою. Підтвердження через `AlertDialog`.
- Повний список пристроїв Supabase клієнту не віддає — у UI це проговорено прямо, замість вигаданого списку.

### Фаза 4 — сповіщення в акаунті (DONE, b3e2e49)

- **Нової моделі не заводили:** реєстр `src/lib/notificationCategories.ts` + `channel_prefs` уже існували
  з Telegram-фази 2 (див. `docs/TELEGRAM_NOTIFICATIONS_DESIGN.md`).
- Матриця винесена в `src/components/notifications/NotificationChannelMatrix.tsx` і підключена на **обидві**
  поверхні — профіль і діалог `NotificationsPage`. Компонент сам вантажить/пише `channel_prefs`, бо дві копії
  логіки розійшлись би, а розходження тут = «вимкнув, а воно шлеться».
- **Тихі години — інформаційний блок, НЕ тумблер.** Вікно 9:00–21:00 Europe/Kiev живе серверною константою
  (`netlify/functions/_lib/quietHours.ts`) і спільне для всієї команди; персонального налаштування не існує,
  тож тумблер був би фальшивим контролом. Константи віддзеркалені у `src/lib/quietHours.ts` (сервер — джерело
  правди), прийом той самий, що з категоріями.
- **ГОЧА:** ролі для видимості категорій — з локального стану сторінки (`memberships_view` по справжньому
  `user.id`), а не з `useAuth()`: у режимі «Дивитись як» той віддає ролі людини, за яку дивляться.
- Push / спливні вікна / звук лишились у стрічці — вони прив'язані до пристрою, а не до акаунта.

### Не роблено (свідомо)

- Heatmap активності на Огляді — з `activity_log` (Пульс). Потрібна перф-оцінка: таблиця велика, знадобиться
  агрегат. Прикраса, не щоденна користь — не тримає реліз.

## 4. Дизайн-мова

Тільки наявні токени: сегментні вкладки з `CountBadge`, тони зі `statusTones.ts`, метри квот як у карток команди,
`tabular-nums` на цифрах, чорна primary-кнопка, картки `rounded-inner border-border/40 shadow-card`. Референс — мокапи в артефакті.

## 4.1 Security-review (фази 1–3)

Пройдено, вразливостей не знайдено. Зафіксовано для майбутніх ітерацій:

- `signOut({ scope: "others" })` не дістає до чужих акаунтів — GoTrue ревокає лише сесії власника токена.
- **Пре-існуюче, не регресія цієї гілки:** `/update-password` — публічний роут, гейтований лише наявністю
  сесії, тож авторизований користувач і до редизайну міг змінити пароль без старого. Інлайн-діалог у профілі
  лише виніс цю здатність на видне місце. Якщо CEO захоче закрити вектор «викрадена сесія → захоплення
  акаунта», потрібне підтвердження поточним паролем (або Supabase «Secure password change») — це продуктове
  рішення, воно додає тертя.

## 5. Відкриті питання CEO

### 5.1 Виплати — знято зі скоупу фази 2

CEO: «виплати поки не роби». Вкладка й картка не реалізовані. Перед поверненням до теми потрібне рішення:

1. Чи бачить рядовий член команди **свої** виплати/бонуси в акаунті? (Зараз payroll — лише Фінанси, owner/SEO.
   Якщо «так» — потрібен окремий self-scoped RPC або RLS-правило + security-review; якщо «ні» — картка лишається за finance-гейтом.)
2. Вкладка «Виробіток» для дизайнерів: показувати бонус у реальному часі чи лише після approve (модель у
   `docs/DESIGNER_PAYROLL_DESIGN.md`)?
