# Команда 2.0 — відсутності, квоти, календар (DESIGN)

Статус: **DRAFT v2 — рішення CEO зафіксовані, чекає фінального «ок» на макети** (потім → план Фази 1).
Макети (ітерація 2, рольові види): артефакт «Команда 2.0 — дизайн-пропозиція»
(https://claude.ai/code/artifact/b54e9196-ed74-4f51-8316-57665980cb74), 01.08.2026.
Код НЕ пишемо до фінального затвердження.

## 0. Зафіксовані рішення CEO (01.08.2026)

1. **Структура** — вкладки «Люди / Календар / Запити» (UnifiedPageToolbar-сегменти); «Мій баланс»
   з варіанта A — перший блок вкладки «Люди» для кожного. Сторінка розходиться за роллю (див. §4).
2. **Квоти** — відпустка 18 / day-off 6 / лікарняні 10 на рік; редагуються персонально (owner/SEO).
3. **Списуються лише робочі дні** — вихідні/свята всередині діапазону ліміт не чіпають
   (виробничий календар `ua_workday_exceptions`, як у нормах дизайнерів).
4. **Залишки приватні** — бачить сама людина + owner і SEO. Факт відсутності в календарі видно всім.
5. **Погодження** — вирішує SEO; owner бачить усе і теж може погодити (Rule 0). Запити самого SEO
   вирішує owner. Лікарняний — без погодження, фіксується фактом, сповіщення одразу.

## 1. Контекст і мета

Сторінка `/team` ([src/pages/TeamPage.tsx](../src/pages/TeamPage.tsx), ~1651 рядок) сьогодні — довідник
людей + журнал відсутностей + календар-діалог. Мета: типи відсутностей із річними квотами,
self-service запити з погодженням, спільний календар-планер і залишки днів — із рольовою видимістю.

## 2. Рев'ю поточного стану (факти з коду)

1. **Два джерела правди про «хто відсутній»** — журнал `tosho.team_absences`
   ([scripts/team-absences.sql](../scripts/team-absences.sql)) і `team_member_profiles.availability_status`
   (+`_start_date`/`_end_date`). Ніде не синхронізовані; тони розійшлися
   (`TeamPage.tsx:134` sick→danger vs `teamAbsences.ts:41` sick→warning).
   KPI «Відсутні» і картка «Сьогодні» читають availability, картка «Відсутності» — журнал.
2. **Немає self-service**: запис у журнал — лише owner/SEO (клієнт `TeamPage.tsx:533`,
   RLS `scripts/team-absences.sql:91-155`). Жодного запиту/погодження у кодовій базі.
3. **Квот немає взагалі.**
4. **Відсутності не породжують сповіщень.** Cron `team-events-reminders.ts` дивиться лише на availability.
5. **Календар — інлайн-діалог** у TeamPage (`:1340-1502`), місячна сітка не компонент.
6. `team_absences` відсутня в `docs/DB_MAP.md`; `docs/modules/team-hr.md` застарілий (рядки/гейти).
7. **Зв'язка з зарплатами**: усі 4 `kind` однаково ріжуть норму дизайнерів
   (`designerPayrollMath.ts:249-255`); базу не ріжуть (рішення CEO 2026-07-26). Див. §5.

## 3. Модель відсутностей

### Типи (рішення §0)

| Тип (`kind`)  | Квота/рік        | Погодження            | Норма дизайнера |
|---------------|------------------|-----------------------|-----------------|
| `vacation`    | 18 роб. днів     | вирішує SEO           | ріже норму, не базу |
| `day_off`     | 6 днів           | вирішує SEO           | ріже норму |
| `sick_leave`  | 10 днів          | **без погодження** — факт + сповіщення | ріже норму |
| `other`       | без ліміту       | вносить лише owner/SEO | ріже норму |

- Життєвий цикл: `pending → approved | declined`, плюс `cancelled` (заявник сам, поки pending).
  Лікарняний і записи owner/SEO створюються одразу `approved`.
- «Хто зараз відсутній» виводиться з журналу (approved-запис, що покриває сьогодні).
  Ручний статус лишається тільки `offline` (фаза 3).
- Вихідні і державні свята — фон календаря, не тип.

### Дані

- **Розширюємо `tosho.team_absences`** (одна таблиця — календар і payroll читають одне джерело):
  `status text not null default 'approved'` check (`pending|approved|declined|cancelled`),
  `requested_by uuid`, `decided_by uuid`, `decided_at timestamptz`, `decision_comment text`.
  Legacy-рядки бекфілимо як `approved`.
- **Нова `tosho.team_absence_quotas`**: `(workspace_id, user_id, year)` PK, `vacation_days`,
  `day_off_days`, `sick_days`, `updated_by/updated_at`. Дефолти 18/6/10 — константа в коді, рядки
  сідяться ліниво; редактор — кнопка «Квоти» на вкладці «Люди» (owner/SEO); новачкам owner/SEO
  може вручну поставити пропорційне число (автопрорейт — не зараз).
- **Аудит `tosho.team_absence_events`** (`absence_id, action, actor_user_id, payload jsonb, created_at`)
  — за патерном `team_member_probation_events`.
- **Залишки — приватні (рішення §0.4)**: квоти/залишки віддаємо через RPC
  (`my_absence_balance(year)` — свій, будь-кому; `team_absence_balances(year)` — усі, лише owner/SEO;
  перевірка ролі всередині RPC). RLS на `team_absence_quotas`: select сам + owner/SEO.
  Голого select для всіх — немає. Сума використаних днів сама по собі виводиться з journal-рядків,
  які й так видно всім, тому приватність тримається саме на квотах/залишках, а UI звичайного
  співробітника балансові блоки колег просто не рендерить.

### RLS / серверна частина

- select журналу: як зараз — будь-який член workspace.
- insert: сам за себе (`user_id = auth.uid()`) лише `status='pending'` і `kind != 'other'`
  (лікарняний — див. нижче); owner/SEO — будь-що (як зараз).
- Лікарняний self-service: insert одразу `approved` АБО pending з автопогодженням у функції —
  вирішимо в плані Фази 2 (схиляюсь до функції: єдиний шлях сповіщень і аудиту).
- update: рішення (pending→approved/declined) — **лише через Netlify-функцію** `team-absence-request.ts`
  за патерном [netlify/functions/team-member-probation.ts](../netlify/functions/team-member-probation.ts)
  (user-scoped auth → authz: job_role='seo' або owner → service-role write → audit event →
  deliverNotifications). Запити SEO вирішує owner (функція перевіряє: заявник seo → рішення лише owner).
  Самому заявнику дозволено тільки pending→cancelled.
- delete: лише owner/SEO вручну (жодних автоматичних видалень).
- Перед мержем — `/security-review` + psql-симуляція ролей.

### Сповіщення

- Нова категорія `team_absences` у **двох** реєстрах: `src/lib/notificationCategories.ts` і
  `netlify/functions/_notificationCategories.ts`.
- Врапери в `src/lib/workflowNotifications.ts`: `notifyAbsenceRequestSubmitted` (→ SEO + owner),
  `notifyAbsenceRequestDecided` (→ заявник), `notifyAbsenceRecorded` (лікарняний → SEO + owner).
  `NotifyUsersParams` (`designTaskActivity.ts:16-23`) розширити полем `category`.
- Доставка — наявний ланцюг `notify-users` → `deliverNotifications` (in-app + push + Telegram).
- Фаза 3 (опційно): кнопки Погодити/Відхилити прямо в Telegram (`telegram-webhook.ts` callback).

## 4. Сторінка (UX) — фінальна структура

Вкладки за конвенцією UnifiedPageToolbar; **той самий роут, вигляд залежить від ролі**:

- **Люди.** Всім: «Мій баланс — рік» (відпустка як сегментований бар «використано / на погодженні /
  вільно», day-off крапками, лікарняні смужкою) + CTA «Запросити відсутність»; «Зараз відсутні»;
  «Події місяця»; стрічка «Найближчі два тижні» (міні-планер відсутніх у вікні); картки людей.
  Рольова різниця: у звичайного співробітника картки колег = контакти + статус-чип + стаж/ДН
  (без балансів); в owner/SEO картки з трьома міні-шкалами залишків + зведення
  «Команда — використання квот» + кнопка «Квоти» (редактор лімітів).
- **Календар.** Всім: планер люди × дні (approved — суцільні бари, pending — штриховані, вихідні/свята
  фоном, сьогодні — підсвічена колонка; тони: vacation→info, sick→warning, day_off→accent, other→neutral —
  уніфікувати константою). Клік по своєму рядку/дню — створення запиту з датою. Owner/SEO додатково:
  смуга «Очікують погодження» під планером з детектором перетинів.
- **Запити.** Співробітник: активний запит (статус, «Скасувати») + історія своїх. Owner/SEO: інбокс
  з фільтром «На погодженні / Вирішені / Мої» + кнопки рішення. Бейдж у табі: у співробітника —
  нейтральний лічильник своїх pending, в owner/SEO — warning-лічильник на погодженні.
- Компоненти виносимо в `src/components/team/`: планер, картка людини (двома варіантами вигляду),
  модалка запиту, рядок запиту. Модуль-ключ `team` лишається `alwaysOn`; гейти — на дії.
  Прибрати дубль `getAvailabilityLabel/BadgeClass` у `TeamMembersPage.tsx:306-313`.

## 5. Зв'язка з payroll — критичний порядок

`designerPayroll.loadAbsences` (`src/lib/designerPayroll.ts:243-265`) і `loadNormPlans` (`:386-395`)
читають `team_absences` без фільтра статусу. **У тому ж чейнджсеті**, де з'являється колонка `status`,
обидва запити отримують `.eq("status","approved")` (бекфіл робить це no-op'ом), інакше pending почне
різати норму. Тести: `designerPayroll.test.ts` + новий кейс «pending не зменшує норму».

## 6. Фази

- **Фаза 1 — сторінка + квоти**: міграція (status + quotas + events, бекфіл), payroll-фільтр approved,
  вкладки/планер/баланси з рольовою видимістю, RPC залишків. Вносять поки що owner/SEO.
- **Фаза 2 — self-service + погодження**: запити будь-кого, функція `team-absence-request.ts`,
  інбокс «Запити», смуга під планером, сповіщення (категорія + врапери + Telegram), скасування свого,
  лікарняний-факт. `/security-review`.
- **Фаза 3 — одна правда + зручності**: availability-бейджі і `team-events-reminders` читають журнал
  (ручний — лише `offline`), детектор перетинів у момент запиту, річний звіт по людині, погодження
  з Telegram (опційно), чистка дублів, оновити `DB_MAP.md` і `team-hr.md`.

## 7. Наступний крок

Фінальне «ок» CEO на макети ітерації 2 → статус ACTIVE → детальний план Фази 1 (writing-plans) → реалізація.
