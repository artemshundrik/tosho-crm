# Зарплата дизайнерів: модель, віджет, адмінка — DESIGN (DRAFT)

> Статус: **чернетка на погодження CEO**. Код ще не пишеться. Модель зі слів CEO
> (2026-07-26) + перевірка на реальних даних прод-БД. Після затвердження —
> реалізація за фазами внизу.

Пов'язане: [SECURITY.md](SECURITY.md) (Rule 0 + RLS-правила),
[CODEX_PROJECT_GUIDE.md](CODEX_PROJECT_GUIDE.md) (модель прав),
Фінанси → «Виплати команді» (існуючий розділ payroll, куди вбудовується адмінка).

---

## 1. Модель компенсації

**Зарплата = фіксована база + змінна частина.** Всі суми до оподаткування.

```
Зароблено(сьогодні) =
    базова_ставка × (робочі_дні_що_минули / робочі_дні_місяця)
  + max(0, візуалів_за_місяць − норма) × ставка_за_візуал
  + Σ (вартість_платного_креативу × відсоток_дизайнера)   // лише статус «Затверджено»
```

- **База** — індивідуальна місячна ставка (напр. 40 000 / 30 000 грн), виставляє
  SEO/owner. Накопичується пропорційно робочим дням (пн–пт, UA-календар).
  Відпустка/лікарняний — лічильник стоїть (діапазони `availability_*` з
  довідника команди).
- **Візуали понад норму** — норма 250 «візуалів» на місяць входить у базу; кожен
  наступний = +100 грн (дефолт). «Візуал» = файл, доданий дизайнером у розділ
  «Результат» дизайн-задачі (події `design_output_upload`).
- **Платні креативи** — задача типу `creative` може бути платною (мін. 4 500 грн
  для клієнта). Дизайнер отримує 30% від вартості проєкту **в момент переведення
  задачі в «Затверджено»**. Відкат статусу → нарахування зникає (розрахунок
  завжди live від поточного стану — окремого «сторно» не треба). Безкоштовні
  креативи йдуть у норму візуалів, грошей не дають.

### Перевірка моделі на реальних даних (липень 2026, станом на 26.07)

| Факт | Значення | Висновок |
|---|---|---|
| Робочих днів у липні | 23 (пройшло 18) | база накопичена на 78% |
| Файли в «Результат»: Лєна / Мар'яна | 220 / 136 | обидві ще під нормою 250; темп Лєни ≈ 281 → буде ~31 понад норму |
| Креативи за липень | 3, усі `pm_review` | нарахування креативів поки 0 — механіка «після затвердження» реалістична |
| Поля оплати креативу в metadata | **немає (0 задач)** | потрібні нові поля + UI |
| Задачі без таймера (2 міс) | 73 із 171 (43%) | час без таймера оцінюємо за власним темпом дизайнера, з позначкою «оцінка» |
| Естімейти задач без таймера | всі = 120 хв | **естімейт — дефолтна заглушка, для грошей непридатний** |

---

## 2. Хто що бачить (жорстко, на рівні БД)

| Роль | Свій віджет | Чужі ставки/суми | Налаштовує тарифи |
|---|---|---|---|
| Дизайнер | так, повна деталізація | ні | ні |
| SEO (`job_role=seo`, access `admin`) | — | так, адмін-сторінка | так |
| Owner / superadmin | — | так (Rule 0: owner бачить усе) | так |
| Менеджер та решта | **нічого** | **ні, навіть через інспектор** | ні |

«Навіть через інспектор» = RLS, а не фронтенд-фільтр (див. §4).

---

## 3. Схема даних (все в `tosho`, нові таблиці)

```sql
-- Індивідуальні тарифи. Історія — рядками: чинний тариф на місяць M =
-- рядок з максимальним effective_from <= першого числа M.
-- Зміна ставки завжди набирає чинності з 1 числа наступного місяця (UI не дає інших дат).
create table tosho.designer_pay_rates (
  id                uuid primary key default gen_random_uuid(),
  workspace_id      uuid not null,
  user_id           uuid not null,
  base_month_rate   numeric(12,2) not null,          -- грн/міс
  visual_norm       int,                              -- override дефолту (null = дефолт)
  over_norm_rate    numeric(12,2),                    -- грн за візуал понад норму
  creative_percent  numeric(5,2),                     -- % дизайнера з платного креативу
  effective_from    date not null,                    -- завжди 1 число місяця
  created_by        uuid not null,
  created_at        timestamptz not null default now(),
  unique (workspace_id, user_id, effective_from)
);

-- Дефолти команди (один рядок на workspace).
create table tosho.designer_pay_defaults (
  workspace_id      uuid primary key,
  visual_norm       int not null default 250,
  over_norm_rate    numeric(12,2) not null default 100,
  creative_percent  numeric(5,2) not null default 30,
  min_creative_cost numeric(12,2) not null default 4500,
  updated_by        uuid, updated_at timestamptz
);

-- Закриття місяця: знімок сум, далі не перераховується заднім числом.
create table tosho.designer_pay_month_close (
  workspace_id uuid not null,
  user_id      uuid not null,
  month        date not null,                         -- 1 число
  base_uah     numeric(12,2) not null,
  over_norm_uah numeric(12,2) not null,
  creatives_uah numeric(12,2) not null,
  total_uah    numeric(12,2) not null,
  details      jsonb not null,                        -- розбивка: списки креативів, лічильники
  closed_by    uuid not null, closed_at timestamptz not null default now(),
  primary key (workspace_id, user_id, month)
);

-- Винятки робочого календаря (свята/перенесення). Порожня на старті:
-- під час воєнного стану державні свята не є вихідними.
create table tosho.ua_workday_exceptions (
  workspace_id uuid not null,
  day          date not null,
  is_workday   boolean not null,     -- false = свято, true = робоча субота
  note         text,
  primary key (workspace_id, day)
);
```

**Платний креатив** — на самій дизайн-задачі (metadata в `activity_log`, як решта
полів задачі): `design_creative_paid: boolean`, `design_creative_project_cost: number`.
Виставляє менеджер у задачі типу «Креатив». Вартість проєкту — бізнес-інформація,
менеджер її і так знає; **відсоток і ставки лишаються тільки в закритих таблицях**.

## 4. RLS (за [SECURITY.md](SECURITY.md), дені-бай-дефолт)

- `designer_pay_rates`, `designer_pay_month_close`:
  - `SELECT`: `tosho.is_workspace_admin(workspace_id) OR tosho.is_workspace_owner(workspace_id) OR user_id = auth.uid()`
  - `INSERT/UPDATE/DELETE`: тільки admin/owner. Дизайнер свої ставки не пише.
  - **Жодної** політики «team member» — менеджер не читає нічого.
- `designer_pay_defaults`: `SELECT` — admin/owner **або** той, хто сам має рядок
  у `designer_pay_rates` (учасник pay-системи бачить правила гри); запис — admin/owner.
- `ua_workday_exceptions`: читають члени команди (несекретне), пише admin/owner.
- Перед мерджем — прогін чеклиста SECURITY.md «simulate the role»: менеджером
  переконатися, що `select` повертає 0 рядків.

## 5. Розрахунок (клієнтська lib `src/lib/designerPayroll.ts`)

Все рахується **live з наявних подій** — нових записів під час місяця немає,
тому відкоти статусів самі прибирають нарахування:

- робочі дні: пн–пт мінус `ua_workday_exceptions(is_workday=false)` плюс робочі
  суботи; мінус дні у діапазонах відпустки/лікарняного з довідника команди;
- візуали: `design_output_upload` за місяць, `uploaded_files.length`, по `user_id`;
- креативи: задачі `design_task_type='creative'` + `design_creative_paid` +
  поточний статус `approved`, виконавець = дизайнер; сума = cost × percent;
- прогноз: повна база + (темп візуалів × робочі дні / минулі − норма, якщо >0) +
  платні креативи в `pm_review`/`client_review` (очікують затвердження);
- час без таймера (перехідний період): оцінка за **власним темпом** дизайнера
  (його ⌀ час типу з таймерних задач; fallback — норма 15/30 хв), скрізь із
  позначкою «оцінка, таймер не вівся». З наступного місяця — правило: без
  таймера робота не рахується в час (лічильники аномалій уже на дашборді).

## 6. UI

### 6.1 Віджет у хедері (дизайнер) — макет затверджується

Живе **поруч із таймер-віджетом** (`DesignerHeaderTimerWidget` в AppLayout);
рендериться лише коли у користувача є чинний рядок `designer_pay_rates`.

- Пігулка: кільце прогресу (зароблено/прогноз) + «31 304 ₴ · ≈ 44 900» + око.
- **Око** маскує всі суми (`•••••`), стан у localStorage; **дефолт — приховано**
  (зарплата у відкритому офісі).
- Клік — поповер (стиль валютного віджета, ~328px): hero-сума + прогноз +
  прогрес-бар; секції База (дні), Візуали (до норми/понад, ⌀ час на візуал),
  Креативи (список, «чекає затвердження»), минулі 3 місяці (з `month_close`);
  плашка «до оподаткування»; «⤢ до значка» — компакт-режим (тільки кільце).
- Макет: https://claude.ai/code/artifact/7f24fd46-7ced-4d92-a8b1-c7c73436ea0c

### 6.2 Налаштування (SEO/owner)

- Дефолти команди — Налаштування компанії.
- Картка дизайнера в «Команді»: базова ставка + overrides; зміни з 1 числа
  наступного місяця; історія — рядки `designer_pay_rates`.

### 6.3 Задача «Креатив» (менеджер)

Перемикач «Безкоштовний/Платний» + вартість проєкту (валідація ≥ min_creative_cost);
прев'ю «Дизайнер отримає N грн після затвердження».

### 6.4 Адмін «Виплати дизайнерам» (SEO/owner)

Вбудовується в **Фінанси → «Виплати команді»** (там уже живе payroll) окремим
блоком/вкладкою: дизайнери × зароблено × прогноз × перевиконання норми;
кнопка **«Закрити місяць»** (snapshot у `month_close`); експорт Excel.

## 7. Захист від накрутки

- Правило в брифі: «1 візуал = унікальний дизайн» (соціальна угода).
- Red-flag у адмінці: ⌀ час на візуал < 5 хв → позначка «можлива накрутка».
- **Знахідка з даних:** макети часто заливаються у 2 форматах
  (`.ai`+`.pdf` = 1.37–1.68 файла на одну роботу), візуали — майже 1:1 (1.06–1.09).
  Рахунок «за файл» системно вигідніший тим, хто ллє два формати. Липень:
  файли 220/136 ↔ унікальні роботи 182/107. → відкрите питання §9.

## 8. Фази реалізації

1. **Схема + віджет (база й візуали).** Таблиці+RLS, налаштування ставок у
   картці дизайнера, дефолти, lib розрахунку, віджет у хедері (без креативів).
2. **Платні креативи.** Поля на задачі, UI менеджера, нарахування/прогноз у віджеті.
3. **Адмінка.** «Виплати дизайнерам» у Фінансах, закриття місяця, Excel, red-flag
   накрутки, історія 3 місяців у віджеті з `month_close`.

## 9. Відкриті питання (потрібне рішення CEO)

1. **Файл чи «робота» для норми?** `XL.ai + XL.pdf` — один макет чи два візуали?
   Модель каже «окремий файл», але це заохочує подвійний експорт (див. §7).
2. **Чи входять макети (layout) у норму 250?** Липень: тільки visualization —
   116/72; разом із layout — 220/136. Схоже, 250 калібровано під «разом» — підтвердити.
3. **Свята:** зараз (воєнний стан) держсвята не вихідні → таблиця винятків
   порожня. Ок?
4. **Дефолт маскування** сум у віджеті: приховано (пропозиція) чи видимо?

## Поза скоупом (зафіксовано)

Податки · бонуси/штрафи поза формулою · банківська інтеграція · перерахунок
минулих місяців при зміні ставки.
