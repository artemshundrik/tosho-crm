---
category: Actions
---

Пасивна пігулка-мітка з іконкою. `icon` — вузол зліва, `active` — увімкнений стан (темнішає нейтрально, не синіє), `size`: `sm` | `md`.

Для ФІЛЬТРА бери не Chip, а `Button variant="chip"` з `aria-pressed` — він тримає стан.
