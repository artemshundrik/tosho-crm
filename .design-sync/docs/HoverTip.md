---
category: Overlays
---

Підказка при наведенні. `label` — вміст бульбашки, `children` — тригер, `side` — бік. `ready={false}` затримує відкриття, поки вміст вантажиться, щоб бульбашка не «стрибала».
