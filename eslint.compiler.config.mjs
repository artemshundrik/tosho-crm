import base from "./eslint.config.js";

/**
 * Окрема конфігурація — перевірка сумісності з React Compiler.
 *
 * Три правила нижче вимкнені в основному конфігу (`eslint.config.js`), і це
 * не помилка: сьогодні компілятор у збірці НЕ ввімкнений, тож у щоденному
 * `npm run lint` вони давали б 34 попередження, які нікому не адресовані.
 *
 * Але вимикати їх назавжди не можна. Заміряно 2026-08-20 офіційним
 * `react-compiler-healthcheck`: компілятор успішно збирає 484 компоненти з
 * 484, тобто НЕ відмовляється від жодного. Це означає, що всі 34 місця він
 * теж скомпілює — і саме там результат може почати відрізнятись від
 * сьогоднішньої поведінки:
 *
 *   purity              — Date.now()/Math.random() у рендері. Компілятор
 *                         мемоїзує результат, і час «застигає» на першому
 *                         обчисленні. Найгірший вид помилки: нічого не падає,
 *                         просто числа неправильні.
 *   immutability        — накопичувач (`let lastLabel`), який мутують у .map().
 *                         Поки все в одному useMemo — працює; коли компілятор
 *                         розділить мемоізацію, роздільники днів поїдуть.
 *   set-state-in-effect — setState в ефекті = зайвий прохід рендеру. На дошці
 *                         дизайну це 268 карток по два таких ефекти кожна.
 *
 * Запускати: `npm run lint:compiler`. Коли всі 34 місця вичищені —
 * перенести правила в основний конфіг як 'error', щоб вони не повернулись,
 * і цей файл видалити.
 */
export default [
  ...base,
  {
    files: ["**/*.{ts,tsx}"],
    rules: {
      "react-hooks/purity": "error",
      "react-hooks/immutability": "error",
      "react-hooks/set-state-in-effect": "error",
    },
  },
];
