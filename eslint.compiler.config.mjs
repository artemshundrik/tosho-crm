import base from "./eslint.config.js";

/**
 * Окрема конфігурація — перевірка сумісності з React Compiler.
 *
 * Три правила нижче вимкнені в основному конфігу (`eslint.config.js`), бо 33
 * наявні порушення робили б `npm run lint` вічно червоним. Ратчет у
 * `scripts/check-compiler-debt.mjs` стежить, щоб борг лише зменшувався.
 *
 * ═══ REQ-90: КОМПІЛЯТОР ПЕРЕВІРИЛИ Й СВІДОМО НЕ ВВІМКНУЛИ (2026-08-21) ═══
 *
 * Перш ніж вмикати його вдруге — прочитай це. Замір уже зроблено, і повторювати
 * його дорого.
 *
 * ЩО ВІН ДАЄ. Заміряно на справжній збірці, а не оцінювачем: 544 функції
 * зібрано, 269 пропущено. Попередній запис тут («484 з 484, не відмовляється
 * від жодної») походив від react-compiler-healthcheck і був неправдою.
 *
 * ЧОМУ ЦЬОГО МАЛО. 79% пропусків — межа самого компілятора 1.0: він не вміє
 * `try/finally` (114) і `throw` всередині `try/catch` (89). У src 633 блоки
 * `try` і 265 `finally`. Наслідок нищівний: у чотирьох найбільших сторінках
 * (DesignTaskPage, QuoteDetailsPage, QuotesPage, DesignPage — 38 тисяч рядків)
 * компілятор зібрав РІВНО НУЛЬ функцій. Тобто саме там, де гальмує, він не
 * робить нічого.
 *
 * ЩО ПОКАЗАВ ЗАМІР ШВИДКОСТІ. Дошка прорахунків, 163 картки, цикли
 * «відфільтрувати → показати всі», однакове вікно, живий вхід, час блокування
 * головного потоку:
 *     з компілятором   161 мс (медіана з 9 вимірів, розкид 110–198)
 *     без компілятора  168 мс (медіана з 6 вимірів, розкид  89–212)
 * Різниця 4% при власному шумі заміру до 138%. Виграшу НЕМАЄ.
 *
 * ЩО ВІН КОШТУЄ. +158 кБ gzip на весь JS, з них +19.5 кБ на вхідний чанк
 * (248.8 → 268.4 кБ), і збірка з 6.4 до 18.4 секунди.
 *
 * КОЛИ ВЕРТАТИСЬ. Коли гігантів розріжуть на менші компоненти без `try/catch`
 * у тілі — тоді компілятору буде за що вхопитись, і замір дасть інше число.
 * Або коли вийде версія компілятора, яка вміє try/catch (у 1.0 не вміє).
 *
 * ═══════════════════════════════════════════════════════════════════════════
 *
 * ЩО ЦЕ ЗМІНЮЄ ДЛЯ ТРЬОХ ПРАВИЛ НИЖЧЕ:
 *
 *   set-state-in-effect — ЗАМІР СПРОСТУВАВ давнє припущення, ніби компілятор
 *                         тут щось зламає. Усі 26 файлів із цим порушенням він
 *                         компілює нормально. Це діагностика зайвого проходу
 *                         рендеру, а не гейт сумісності: тримаємо ратчетом
 *                         заради швидкості.
 *   purity              — Date.now()/Math.random() у рендері. Ось це справді
 *                         небезпечно, якщо компілятор колись увімкнуть: він
 *                         мемоїзує результат, і час «застигає». Порушень 0.
 *   immutability        — накопичувач, який мутують у .map(). Лишилось одне
 *                         місце (picker-input), піде разом із forwardRef.
 *
 * І ГОЛОВНА ПАСТКА: у компонентах із `try/catch` у тілі лінт СЛІПИЙ — він
 * замовкає там повністю (facebook/react#35644, відкритий з 27.01.2026). Тому
 * в тих самих чотирьох гігантах він знаходить 0 порушень, хоч у них 132, 145,
 * 112 і 80 useState. Число «борг 33» — це не правда про код, а межа зору.
 *
 * Запускати: `npm run lint:compiler`.
 */
export default [
  ...base,
  {
    files: ["**/*.{ts,tsx}"],
    rules: {
      "react-hooks/purity": "error",
      "react-hooks/immutability": "error",
      "react-hooks/set-state-in-effect": "error",
    },
  },
];
