/**
 * Впізнавання накопичувача дрібниць — окремим модулем БЕЗ ЖОДНИХ ІМПОРТІВ.
 *
 * НАВІЩО ОКРЕМО, А НЕ В queueShelves. Це знання потрібне не лише інтерфейсу:
 * без нього дія `commit` ставить накопичувачу статус, а деплой його викочує —
 * і напрям зникає назавжди (§4.5 docs/DEV_REQUESTS_DESIGN.md). Тобто те саме
 * правило мусить бути видиме й у Netlify-функції.
 *
 * А `queueShelves` для функції заважкий: через `types.ts` він тягне `react` і
 * `lucide-react`, та ще й аліас `@/`, якого збірка функцій не знає. Дублювати
 * рядок «Дрібниці:» у другому місці не можна поготів — два переліки того
 * самого розходяться, і розійдуться вони мовчки.
 *
 * Тому предикат бере `{ title }`, а не `DevRequest`: так модуль лишається
 * листком, який можна імпортувати звідусіль.
 */

/**
 * Позначка картки-накопичувача дрібниць.
 *
 * НАВІЩО ВОНИ ВЗАГАЛІ. Замір 26.08.2026 пояснив, звідки береться приплив у сім
 * карток на день: 59 викочених карток зі 103 закрились ОДНИМ комітом. Тобто
 * більшість — це дрібниця, якій картка з темою, типом, зоною, напрямком і
 * пріоритетом важча за саму роботу. Практика зветься papercuts і давно
 * відпрацьована в Ubuntu, GitHub і Linear: дрібний баг не заводить окремої
 * задачі, а стає рядком у постійному списку свого напряму. Такі картки не
 * закриваються ніколи — їх розгрібають пачкою, коли доходять руки.
 *
 * ЧОМУ ЗА НАЗВОЮ, А НЕ ЗА НОВИМ ПОЛЕМ. Прапорець у базі довелось би заводити
 * міграцією, тримати у формі й пояснювати — заради ознаки, яку людина й так
 * бачить у назві. Префікс читається однаково і в CRM, і в Telegram, і в списку
 * запитів, а картка лишається звичайною карткою: її можна фільтрувати,
 * коментувати й закрити, якщо напрям віджив.
 */
export const PAPERCUT_PREFIX = "Дрібниці:";

/** Чи це накопичувач дрібниць. Регістр і зайві пробіли значення не мають. */
export function isPapercutCard(card: { title: string }): boolean {
  return card.title.trim().toLowerCase().startsWith(PAPERCUT_PREFIX.toLowerCase());
}

/** Назва напряму з назви картки: «Дрібниці: мова інтерфейсу» → «мова інтерфейсу». */
export function papercutLabel(card: { title: string }): string {
  const title = card.title.trim();
  return title.slice(PAPERCUT_PREFIX.length).trim() || title;
}

/**
 * Накопичувач, у який ще нічого не поклали.
 *
 * Такий не показують на дошці: полиця без дрібниць не є ні задачею, ні
 * роботою — вона лише займає місце серед справжніх карток. Наповнюють її у
 * «Черзі», на полиці «Дрібниці», звідки вона нікуди не зникає (рішення Артема
 * 28.08.2026).
 */
export function isEmptyPapercut(card: { title: string; checklist: unknown[] }): boolean {
  return isPapercutCard(card) && card.checklist.length === 0;
}
