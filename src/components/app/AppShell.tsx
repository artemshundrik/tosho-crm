import { AppPageLoader } from "@/components/app/AppPageLoader";

export function AppShell() {
  return <AppPageLoader title="Завантаження CRM" subtitle="Перевіряємо сесію та відкриваємо робочий простір." />;
}
