/**
 * Лого сервісу чи постачальника за доменом — фавікон через Google.
 * Одна копія на CRM: досі та сама адреса була в «Інтеграціях» і в лого
 * підписок у Фінансах, а третю (для постачальників) заводити не стали.
 */

/**
 * Домени, для яких лого лежить у нас, а не тягнеться з Google.
 *
 * НАВІЩО ПЕРЕКРИТТЯ. Google віддає фавікон сайту, а він у половини B2B-порталів
 * або відсутній, або це сірий аркуш за замовчуванням — на картці постачальника
 * така заглушка виглядає як «щось не завантажилось». Папірус саме такий
 * (09.09.2026): свого фавікона в них немає.
 *
 * Кладемо ФАЙЛ, а не адресу чужого сайту: логотип із їхнього сервера відпаде
 * разом із їхнім редизайном, та ще й тягнеться повз наш кеш. Файл у `public/`
 * віддається з нашого домену й переживає будь-які їхні зміни.
 *
 * Картинка має бути КВАДРАТНОЮ: слот у картці й у шапці — квадрат, і широкий
 * логотип із написом у ньому сплющиться. Логотипи-стрічки ріжемо до самої
 * іконки, а цілий файл кладемо поруч із суфіксом `-full` — на випадок, коли
 * знадобиться місце під повний знак (у документі, у листі).
 */
const LOCAL_LOGOS: Record<string, string> = {
  "papirus-opt.com": "/brand/suppliers/papirus.png",
};

export const faviconUrl = (domain: string): string =>
  LOCAL_LOGOS[domain] ?? `https://www.google.com/s2/favicons?domain=${encodeURIComponent(domain)}&sz=128`;
