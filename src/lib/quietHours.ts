// Тихі години — ДЗЕРКАЛО netlify/functions/_lib/quietHours.ts для фронтенду.
// Тримати синхронно (той самий прийом, що з notificationCategories.ts).
//
// Джерело правди — серверний файл: саме він вирішує, слати сповіщення чи ні.
// Тут значення потрібні лише для того, щоб UI міг чесно показати вікно, а не
// вигадувати своє. Персонального налаштування немає: вікно спільне для всіх,
// тож у профілі це інформаційний блок, а не тумблер.

export const QUIET_HOURS_TIME_ZONE = "Europe/Kiev";

/** З 9:00 включно і до 21:00 — вікно, коли денні сповіщення дозволені. */
export const NOTIFY_FROM_HOUR = 9;
export const NOTIFY_UNTIL_HOUR = 21;
