# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tsbuildinfo
.env.backup
backups/
tmp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Netlify folder
.netlify

# Claude Code local-only settings (per-machine permissions, env)
.claude/settings.local.json
CLAUDE.local.md

# one-off backfill backups (contain prod data)
scripts/.backfill-quote-totals.backup.*.json
scripts/.backfill-order-sale-prices.backup.*.json

# Робочі артефакти міграції Dropbox-інбоксу: журнал прогону, звіт звірки,
# заповнена таблиця рішень. Це стан конкретного запуску, не код.
dropbox-inbox-*.json
inbox-plan.csv


# design-sync: staged converter + generated bundle + machine state.
# Довговічне (config.json, NOTES.md, conventions.md, previews/, overrides/) — комітиться.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Дерево типів для design-sync (генерується tsconfig.types.json)
types/

# Замок Deno. Його переписує netlify dev: CLI тягне bootstrap edge-функцій і
# заразом дзеркалить сюди залежності з package.json. Edge-функцій у нас немає
# жодної, збірка цей файл не читає — лишався тільки шум у git status і фальшиві
# зміни в комітах. З'явиться перша edge-функція — повернути під git.
deno.lock

# Наскрізні перевірки (REQ-140). У .auth/ лежить СЕСІЯ живого облікового
# запису — доступ до проду в чистому вигляді, а репозиторій публічний.
# Звіти й сліди — артефакти конкретного прогону, не код.
.auth/
playwright-report/
test-results/
blob-report/

# Аркуш станів лейауту — знімки для ока, не для репозиторію
e2e/__screens__/
