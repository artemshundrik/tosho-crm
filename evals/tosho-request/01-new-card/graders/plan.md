---
type: llm
focus: last_message
---
Відповідь описує заведення НОВОЇ картки саме через ендпоінт
`dev-request-capture` (адреса виду https://tosho.pro/.netlify/functions/dev-request-capture),
POST із полем `text`, у якому текст скарги переданий словами автора.

Провал, якщо: названо `dev-request-board`; текст переписано/скорочено замість
передачі як є; напрямок (`module`) або пріоритет проставлені самостійно —
скіл прямо забороняє це робити, їх ставить сервер.
