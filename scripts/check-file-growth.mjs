#!/usr/bin/env node
/**
 * Ратчет розміру: великі файли можуть лише зменшуватись, нові гіганти не родяться.
 *
 * НАВІЩО. Це єдина перевірка в наборі, яка б'є не по наслідку, а по причині.
 *
 * Усе, з чим ми воювали в REQ-96 і REQ-109, — сліпий лінт, заглушки, борг перед
 * компілятором, години на розплутування одного ефекту — росло з одного кореня:
 * файли на 6–13 тисяч рядків. У такому файлі 145 `useState`, і жодна людина не
 * тримає в голові, що з чим повʼязано. Лінт там теж не помічник: у чотирьох
 * найбільших сторінках React Compiler збирає рівно нуль функцій, бо спотикається
 * на `try/finally` і `throw` всередині `try/catch`.
 *
 * Жоден лінтер цього не лікує. Лікує лише те, що не дає файлу рости далі.
 *
 * ДВА ПРАВИЛА
 *
 * 1. Файл зі списку нижче не має права стати більшим. Список — це знімок на день
 *    заведення ратчета: усе, що вже перетнуло 2 000 рядків.
 * 2. Файл, якого в списку немає, не має права дорости до 2 000. Новий гігант не
 *    зʼявиться непомітно — а саме так зʼявились нинішні.
 *
 * ЩО РОБИТИ, КОЛИ ВПАЛО. Майже завжди відповідь та сама: нове йде в ОКРЕМИЙ
 * модуль, а не дописується в кінець. Запити до бази — у `queries.ts` поруч зі
 * сторінкою, чисті помічники — на рівень модуля або в `src/lib`, шматок розмітки
 * — в окремий компонент у `components/`. Так у REQ-109 з картки прорахунку
 * поїхало 600 рядків і не поверталось.
 *
 * Піднімати число можна, але це свідоме рішення, а не спосіб пропустити пуш:
 * якщо піднімаєш — напиши в коміті, чому цей код не міг жити окремо.
 *
 * ЧОМУ ЦЕЙ РАТЧЕТ МʼЯКШИЙ ЗА `check-compiler-debt.mjs`. Той падає і тоді, коли
 * борг ЗМЕНШИВСЯ, — щоб число одразу зафіксували. Тут так не можна: розмір
 * коливається на кожній правці, і падіння через «файл схуднув на три рядки»
 * привчило б обходити перевірку. Тому зменшення лише нагадує про себе в
 * консолі, а зупиняє пуш тільки зростання.
 */
import { readdirSync, statSync, readFileSync, writeFileSync } from "node:fs";
import { join } from "node:path";

/** З якого розміру файл вважається великим і потрапляє під нагляд. */
const GIANT_THRESHOLD = 2000;

/**
 * Стеля для кожного файлу, що вже був великим на 2026-08-22.
 *
 * ПІДНЯТО 23.08.2026 для двох файлів — свідомо, а не щоб зробити ратчет
 * зеленим. QuoteDetailsPage виріс на 198 рядків у 638b002 і 127709a
 * (редагування позиції прорахунку + вирівняні поля тиражу), AppLayout — у
 * роботі над «дивитись як» плюс 6 рядків на пункт меню «Стек» (REQ-116).
 * Це доробки, а не розповзання: у кожній з них новий код лежить там, де вже
 * живе відповідна логіка. Але стеля — це обіцянка, а не формальність: якщо
 * її доведеться підняти ще раз, спершу винось модуль.
 *
 * ПІДНЯТО 25.08.2026 для двох файлів на +11 і +1 рядок. Обидві зміни — доробки
 * наявних місць, а не новий код у кінці: у AppLayout це каркас бокового меню
 * (плашка заголовка тримає висоту справжнього, щоб меню не підстрибувало, поки
 * доїжджають доступи) плюс крок відступу між пунктами, у toshoApi — розбір
 * помилки в уже наявному запиті наборів. Виносити тут нічого: розмітка сайдбара
 * за визначенням живе в AppLayout, а гілка помилки — поруч зі своїм запитом.
 *
 * У тій самій роботі QuoteDetailsPage пішов у ІНШИЙ бік: редизайн картки додав
 * ~300 рядків, і замість підняття стелі з них зробили чотири модулі поруч зі
 * сторінкою — QuotePartyCard, QuotePriceSummary, QuoteStatusControl і
 * EconomicsComingSoon. Файл вийшов меншим, ніж був до роботи.
 *
 * ПЕРЕРАХОВАНО 24.08.2026 після роботи над швидкістю дошок (картка 136).
 * Спершу винесено модулі, як і вимагає рядок вище: `useKanbanViewportHeight`
 * і `useDeferredHeavySurface` забрали з обох дошок по вісімдесят рядків
 * дослівно однакового вимірювання й відкладеного першого кадру. Тому
 * DesignPage не піднято, а ОПУЩЕНО: 6559 -> 5887. Решта — залишок, який у
 * спільний модуль не виноситься: розведення мобільної й десктопної гілок
 * (+28 у QuotesPage) і по два-пʼять рядків пояснень там, де читання кешу
 * переїхало в useMemo.
 *
 * ПІДНЯТО 24.08.2026 для двох дошок — REQ-138, винесення скасованих карток із
 * канбанів в окремий список. Спершу винесено все, що виноситься, як і вимагає
 * рядок вище: обидва списки (`CancelledQuotesList`, `CancelledDesignTasksList`)
 * і дія повернення (`restoreQuote.ts`). Те, що лишилось у сторінках, окремо
 * жити не може: це стан вигляду, який читається з фільтра статусу тієї ж
 * сторінки, і гілка розмітки, яка вибирає між дошкою та списком.
 *
 * Числа менші, ніж були в першій спробі: звідти прибрано перемикач «Дошка /
 * Скасовані» в тулбарі. Він займав постійне місце на екрані заради дії раз на
 * рік, дублював фільтр статусу й перезавантажував дошку на кожне натискання.
 * Шлях до скасованих — той самий фільтр, що був завжди.
 *
 * ПІДНЯТО 25.08.2026 — замовлення за собівартістю й вибір погодженого тиражу.
 * Спершу винесено все, що виноситься: гроші замовлення поїхали з
 * `orderRecords` в окремий `orderItemPricing` (і аж тоді отримали тести —
 * усередині orderRecords їх не написати, він тягне клієнт Supabase), а правило
 * «один погоджений тираж на позицію» — у `lib/quoteRuns`
 * (`applyApprovedRunToggle`). Те, що лишилось у чотирьох файлах, окремо жити
 * не може: одне поле, протягнуте крізь чотири мапери читання й запису
 * (toshoApi), гілка розмітки з кнопкою «Погодив клієнт» усередині дерева
 * позиції (QuoteDetailsPage), два ранні виходи в обробниках дошки
 * (QuotesPage) і блокер готовності поруч із рештою блокерів (orderRecords).
 *
 * ПІДНЯТО 30.08.2026 для toshoApi (+18) — поле накрутки (картка 149). Той самий
 * випадок, що описаний нижче: ОДНЕ поле, протягнуте крізь чотири мапери читання
 * й запису, перелік колонок і запасний шлях на старішу схему. Виносити нема
 * чого — мапер рядка тиражу не існує окремо від запиту, який його читає.
 *
 * ПІДНЯТО 25.08.2026 ще раз, і лише для toshoApi (+19): рецензія знайшла, що
 * перенесення позначки «погоджено клієнтом» валило весь запис (частковий
 * унікальний індекс перевіряється порядково й не буває DEFERRABLE). Виправлення
 * — три рядки сортування, решта пʼятнадцять — пояснення, чому воно там стоїть.
 * Саме такі коментарі й економлять наступний вечір: без них сортування виглядає
 * як випадковість і зникає при першому ж рефакторингу.
 *
 * І ще +7 QuotesPage: тост гуртової зміни перестав рахувати картки, яких гейт
 * не чіпає. Винести нема чого — це фільтр і рядок тексту в тому самому
 * обробнику.
 *
 * ПІДНЯТО 25.08.2026 для AppLayout (+16) — повернення кнопки «хто онлайн» у
 * шапку на десктопі (ef31f14). Прибираючи її з телефона, вирізали розмітку
 * цілком, і кнопка зникла на обох ширинах. Код, що повернувся, окремо жити не
 * може: це гілка тернарника мобільної/десктопної шапки та монтування вже
 * наявного компонента з `workspace-presence-widgets`, тобто виносити нема чого
 * — воно вже винесене. Більшість приросту — коментар про те, ЧОМУ кнопка стоїть
 * першою в групі (REQ-25: група росте вліво й штовхає курс на 51 px), і саме
 * цей коментар не дасть переставити її назад.
 *
 * ПІДНЯТО 25.08.2026 для трьох файлів — скарга дизайну на завантаження й на
 * гейт «Затверджено замовником» (REQ-152, REQ-153) плюс робота над боковим меню.
 *
 * DesignTaskPage (+26) і DesignPage (+3). Спершу винесено все, що виноситься:
 * правило «чого вимагає цей тип задачі» поїхало з картки в `designTaskStatus`
 * (`resolveApprovalRequirements`, `getMissingApprovalKind`) — воно лежало там
 * копією, а правило у двох місцях рано чи пізно розходиться; заразом воно
 * нарешті отримало тести. Те, що лишилось у сторінках, окремо жити не може:
 * перехід на потрібний таб усередині обробника статусу (чотири виклики
 * сеттерів), гілка тексту в смузі розділу й рядок `description` у тості на
 * канбані.
 *
 * AppLayout (+308) — закріплення пунктів меню й хореографія згортання
 * (a15b805, 07a85bb, c60674d). Чиста частина стану вже винесена окремим
 * модулем `lib/sidebarPinnedState.ts`; решта — розмітка й анімація самого
 * лейауту, яка поза ним не існує. Це найбільший стрибок за час ратчета: якщо
 * бокове меню поповзе далі, наступний крок — не стеля, а окремий компонент.
 *
 * Перші чотири — ті самі сторінки-гіганти, заради яких усе й затівалось.
 * Скорочувати їх ніхто не зобовʼязаний одним заходом; головне, щоб не росли.
 *
 * ПІДНЯТО 26.08.2026 для AppLayout (+10) — хореографія згортання бокового меню
 * без перельоту (2281eea). Сама правка мала: крива, дві тривалості й затримка
 * другої фази. Приросло здебільшого ПОЯСНЕННЯ, чому перельоту немає: у макеті
 * стояла пружина, ширина перескакувала ціль приблизно на 14px і верталась, і
 * саме це визнали зайвим. Без цих рядків наступний, хто відкриє файл, побачить
 * звичайну криву й поверне пружину назад — тобто коментар тут коштує дешевше за
 * повторну правку. Виносити нема чого: це рядок класів на самому лейауті.
 *
 * Якщо бокове меню поповзе далі — межу більше не піднімати, а різати на
 * окремий компонент, як і сказано вище про попередній стрибок на +308.
 *
 * ПІДНЯТО 27.08.2026 для DesignTaskPage (+45) — гейт «не повертаємо на правки
 * без правки» (REQ-187). Скарга Влада: менеджер пише правку, тисне «Повернути
 * на правки», задача йде дизайнеру — а правка лишається ненадісланою
 * чернеткою.
 *
 * Спершу винесено все, що виноситься, і вийшло три модулі замість рядків у
 * сторінці: правило свіжості правки й розвилка гейту — у `designTaskStatus`
 * (`hasFreshChangeRequest`, `resolveChangesGate`, з тестами, бо той самий гейт
 * копіює канбан); діалог — у `components/design/UnsentChangeRequestDialog`;
 * стан, побічні дії та орквстрація «надіслати, а потім за потреби довести
 * статус» — у `useChangeRequestGuard` там-таки. Заразом звідти прибралась
 * копія `CHANGE_REQUEST_AUTO_CHANGES_STATUSES`.
 *
 * Те, що лишилось у сторінці, окремо жити не може: сам виклик гейту всередині
 * `updateTaskStatus` (це шов, через який проходять три двері з чотирьох),
 * `createBriefChangeRequest`, яка тепер повертає «надіслалось?», і монтування
 * діалогу. Виносити з них нема чого — воно вже винесене.
 *
 * ПІДНЯТО 27.08.2026 для OrdersCustomersPage (+4) — підказка про схожі компанії
 * під полем назви (REQ-189). Приріст рівно в тому, що це: два пропси на два
 * діалоги. Сама підказка живе окремо (`components/customers/CompanyDuplicateHint`),
 * правило схожості — теж (`lib/companyDuplicateHint` з тестами на справжніх
 * назвах із бази). У сторінці лишилось передавання `teamId` і id картки, яку
 * редагують, — виносити тут нічого.
 *
 * ПІДНЯТО 30.08.2026 для QuoteDetailsPage (+22) — попередження про порядок
 * дедлайнів (REQ-155 p8). Того ж дня стеля цього файлу опускалась двічі, з 9929
 * до 8512, тож підняття повертає назад дві сотих того, що звільнили.
 *
 * Спершу винесено все, що виноситься, як і вимагає рядок вище: правило
 * «макет пізніше за відповідь» поїхало в `deadlineLabels` і отримало тести на
 * чотири випадки, розмітка попередження — в окремий `QuoteDeadlineOrderWarning`.
 * У сторінці лишилось те, що поза нею не існує: два значення, зчитані з
 * ЧЕРНЕТКИ полів дати (вони живуть у стані цієї ж сторінки), стан керованої
 * підвкладки й монтування компонента.
 *
 * ПІДНЯТО 01.09.2026 — REQ-182, тип угоди й дно накрутки за ним. Спершу винесено
 * все, що виноситься: правило шкали живе в `src/lib/quoteDealType.ts` (з
 * тестами), а вибір типу — окремим компонентом `QuoteDealTypePicker`, бо він і
 * так потрібен двічі: при створенні прорахунку й при його редагуванні з картки.
 * Те, що лишилось у цих чотирьох файлах, окремо жити не може — це протягування
 * `dealType` крізь наявні виклики: параметр гука, проп трьох компонентів, поле
 * в payload створення й правки, гілка гейта на кожен прорахунок комплекту.
 * Виносити «протягування» немає куди: воно за визначенням лежить там, де стоїть
 * виклик. У білдері прорахунків до нього додався контрол вибору типу — він
 * стоїть у ряду чіпів шапки поруч із валютою й дедлайном, бо описує ту саму
 * сутність, що й вони: замовлення цілком.
 *
 */

const CEILINGS = {
  "src/pages/DesignTaskPage.tsx": 12874,
  "src/pages/QuoteDetailsPage.tsx": 7477,
  "src/pages/QuotesPage.tsx": 8459,
  "src/pages/DesignPage.tsx": 6012,
  // +1 рядок 23.08.2026: доданий імпорт типів таблиць. Це той рідкісний випадок,
  // коли зростання файлу зменшує ризик — два payload на 40 полів кожен
  // перестали бути `Record<string, unknown>` і тепер звіряються з базою.
  "src/pages/OrdersCustomersPage.tsx": 4277,
  "src/pages/TeamMembersPage.tsx": 2633,
  "src/pages/OrdersProductionDetailsPage.tsx": 3067,
  "src/components/quotes/QuoteBatchBuilderDialog.tsx": 2936,
  "src/features/finances/FinanceExpenses.tsx": 2439,
  "src/layout/AppLayout.tsx": 3000,
  "src/components/quotes/NewQuoteDialog.tsx": 2758,
  "src/features/tosho-ai/ToShoAiConsole.tsx": 2721,
  "src/components/design/DesignersDashboard.tsx": 2702,
  "src/features/orders/orderRecords.ts": 2614,
  "src/lib/toshoApi.ts": 2634,
  "src/pages/TeamPage.tsx": 2427,
  "src/pages/ProfilePage.tsx": 2156,
  "src/features/catalog/ProductCatalogPage/hooks/useModelEditor.ts": 2079,
};

/**
 * Згенероване — не наш код, ділити його немає сенсу й нікому не допоможе.
 * `database.types.ts` пише `supabase gen types`.
 */
const GENERATED = new Set(["src/lib/database.types.ts"]);

/** Наскільки файл має схуднути, щоб про це варто було нагадати. */
const NOTABLE_SHRINK = 100;

function collectSourceFiles(dir) {
  const out = [];
  for (const entry of readdirSync(dir)) {
    const full = join(dir, entry);
    if (statSync(full).isDirectory()) out.push(...collectSourceFiles(full));
    else if (/\.(ts|tsx)$/.test(full)) out.push(full);
  }
  return out;
}

const countLines = (file) => readFileSync(file, "utf8").split("\n").length;

const grown = [];
const born = [];
const shrunk = [];

for (const file of collectSourceFiles("src")) {
  if (GENERATED.has(file)) continue;
  const lines = countLines(file);
  const ceiling = CEILINGS[file];

  if (ceiling === undefined) {
    if (lines >= GIANT_THRESHOLD) born.push({ file, lines });
    continue;
  }
  if (lines > ceiling) grown.push({ file, lines, ceiling });
  else if (ceiling - lines >= NOTABLE_SHRINK) shrunk.push({ file, lines, ceiling });
}

// --fix (він же `npm run docs:sync`) опускає стелю сам. Це похідне число, і поки
// його вписували руками, воно давало окремі коміти «стеля підтягнута під…» —
// чотири таких за чотири дні (замір 27.08.2026). Опускати можна автоматом:
// ратчет від цього лише міцніє. ПІДІЙМАТИ — ніколи, це рішення людини.
const ЛАГОДИМО = process.argv.includes("--fix");

if (ЛАГОДИМО && shrunk.length > 0) {
  const self = new URL(import.meta.url).pathname;
  let source = readFileSync(self, "utf8");
  for (const { file, lines, ceiling } of shrunk) {
    const рядок = new RegExp(`("${file.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")}":\\s*)${ceiling}`);
    source = source.replace(рядок, `$1${lines}`);
    console.log(`[розмір] стеля опущена: ${file} ${ceiling} → ${lines}`);
  }
  writeFileSync(self, source, "utf8");
}

for (const { file, lines, ceiling } of shrunk) {
  if (ЛАГОДИМО) continue;
  console.log(
    `[розмір] ${file} схуднув: ${lines} замість ${ceiling}. Опусти стелю: npm run docs:sync.`
  );
}

if (grown.length === 0 && born.length === 0) {
  console.log(`[розмір] під наглядом ${Object.keys(CEILINGS).length} файлів, жоден не виріс`);
  process.exit(0);
}

for (const { file, lines, ceiling } of grown) {
  console.error(`[розмір] ✖ ${file} виріс: ${lines} рядків замість ${ceiling} (+${lines - ceiling}).`);
}
for (const { file, lines } of born) {
  console.error(`[розмір] ✖ ${file} доріс до ${lines} рядків і стає новим гігантом.`);
}
console.error("[розмір] Винеси нове в окремий модуль — як це робиться, написано в шапці цього файлу.");
process.exit(1);
